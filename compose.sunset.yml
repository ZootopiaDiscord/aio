services:
  aio:
    networks:
      - reverse-proxy
    labels:
      caddy: zootopia.chat

  app:
    networks:
      - reverse-proxy
    labels:
      caddy: app.zoot.tawmy.dev

networks:
  reverse-proxy:
    external: true